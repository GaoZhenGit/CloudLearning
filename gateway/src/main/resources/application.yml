server:
  port: 20100
spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:20000
    gateway:
      discovery:
        locator:
          enabled: true # 让gateway可以发现nacos中的微服务